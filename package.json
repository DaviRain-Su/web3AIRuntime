{
  "name": "web3-ai-runtime",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "sh -c 'cd packages/workflow && bun run build' && sh -c 'cd packages/policy && bun run build' && sh -c 'cd packages/trace && bun run build' && sh -c 'cd packages/chains && bun run build' && sh -c 'cd packages/core && bun run build' && sh -c 'cd packages/pi && bun run build'",
    "test": "bun run --workspaces test",
    "lint": "bun run --workspaces lint",

    "pi": "pi -e packages/pi/dist/pi-extension.js"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.0",
    "typescript": "^5.4.5"
  }
}
